// Jest setup for Everyday Elo Mobile - Unit Tests
// These are minimal mocks for running pure function tests

// Signal to React 19's act() that this environment supports it. Without this,
// react-test-renderer logs noisy warnings on every state update inside act().
globalThis.IS_REACT_ACT_ENVIRONMENT = true;
